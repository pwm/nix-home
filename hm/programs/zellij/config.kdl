pane_frames false

stacked_resize false

// zellij 0.45 switches to Scroll mode on mouse wheel scrolling by default, so keys
// typed after scrolling up do not reach the shell. Keep the old behaviour.
scroll_mode_sync false

ui {
  pane_frames {
    hide_session_name true
    rounded_corners true
  }
}

keybinds {
  normal {
    bind "Alt t" { NewTab; }
    bind "Alt ;" {
      LaunchPlugin "filepicker" {
        floating true
        close_on_selection true
      }
    }
    bind "Ctrl `" { SwitchToMode "Locked"; }
    unbind "Ctrl g"
  }

  locked {
    bind "Ctrl `" { SwitchToMode "Normal"; }
  }
}
